version: "3.8"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_ENV: ${APP_ENV:-staging}
    image: ocs-portal-web_frontend:latest
    container_name: ocs-web
    ports:
      - "5173:5173"
    restart: unless-stopped